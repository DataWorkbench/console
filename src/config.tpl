(function() {
    if (!window.CONFIG_ENV) {
        window.CONFIG_ENV = {
            PROXY_API_URL: '{PROXY_API_URL}',
            THEME: '{THEME|default}',
            IS_PRIVATE: {IS_PRIVATE|true}
        }
    }
})()

import tw, { css } from 'twin.macro'

const pageTabStyles = css`
  .page-tab-container .collapse-panel .tab-title {
    ${tw`font-medium`}
  }
  .collapse-panel .panel-right .icon {
    ${tw`-top-12`}
  }
`

export default pageTabStyles
